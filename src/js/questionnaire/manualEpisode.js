import { groupsData } from "../timeline/timelineData";
import { surveyService } from "../stateMachine/stateMachine";



function getTrajectoires() {
  // Filtrer groupsData pour garder seulement les groupes 
  // qui ont la propriété "nestedGroups"
  // ???
  }